public static class Enums {
    public enum CollectionType {
        Animals
    }

    public enum NeighbourType {
        Top,
        Left,
        Front,
        Right,
        Back,
        Bottom
    }
}
